services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: streambox
      POSTGRES_PASSWORD: streambox
      POSTGRES_DB: streambox
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
